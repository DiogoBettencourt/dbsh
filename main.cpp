#include <iostream>
#include <string>


int main() {
    bool end = false;

    while (!end) {
        std::string input;
        std::cout << "dbsh> ";
        std::getline(std::cin, input);
    }
    
}